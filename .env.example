SERVER_PORT=8080
DB_USERNAME=root
DB_PASSWORD=110104
DB_URL=jdbc:mysql://localhost:3306/user_management_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Ho_Chi_Minh

# JWT (nên dùng chuỗi ngẫu nhiên >= 32 ký tự)
JWT_SECRET=change_me_to_a_long_random_secret
JWT_EXPIRED_ACCESS=3600000
JWT_EXPIRED_REFRESH=2592000000

# Cấu hình Email gửi đi (ứng dụng Gmail + app password)
MAIL_USERNAME=yourmail@gmail.com
MAIL_PASSWORD=your_app_password_16chars

# Cấu hình Redis (Upstash hoặc local)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=