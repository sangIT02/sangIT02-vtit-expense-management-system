version: '3.8'

services:
  user-management-app:
    build:
      context: .
    container_name: user_management_container
    ports:
      - "8080:8080"
    # Lấy toàn bộ biến trong file .env và bơm vào container
    env_file:
      - .env